---

import ArticleItem from "./ArticleItem.astro";
const { allPosts } = Astro.props;

---


<ul class="article-grid">
    {allPosts.map((post: any) => (
        <ArticleItem {...post.frontmatter} url={post.url} />
    ))}

</ul>